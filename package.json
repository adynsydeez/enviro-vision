{
  "name": "enviro-vision-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"uvicorn api:app --reload --port 8000 --app-dir backend\"",
    "install:all": "npm install --prefix frontend && pip install -r backend/requirements.txt",
    "lint": "npm run lint --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
